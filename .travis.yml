language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  include:
    - os: linux
      before_install:
        - sudo apt-get -qq update
        - sudo apt-get install -y libxtst-dev
  allow_failures:
    - rust: nightly
os:
  - linux
  - osx

